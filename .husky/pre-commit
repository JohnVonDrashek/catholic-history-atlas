#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run lint-staged for formatting and linting
npx lint-staged

# Run data validation checks
npm run lint:duplicates || exit 1

echo "✓ All pre-commit checks passed!"
